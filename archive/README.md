# Archived HTML (not deployed)

These files are **draft / alternate** pages kept for future use. They are **not** linked from the live `index.html`, **excluded** from `sitemap.xml` and `llms.txt`, and should **not** be uploaded to production hosting unless you intentionally restore them.

| File | Notes |
|------|--------|
| `wellness.html` | Wellness narrative page |
| `plans.html` | Roadmap page (distinct from homepage `#plans` section) |
| `support.html` | Support/donation narrative page |
| `about.html` | Redirect stub to wellness (legacy) |

To restore: move the desired file(s) to the site root, add nav/footer links, and re-list URLs in `sitemap.xml` and `llms.txt` if they should be indexed.
